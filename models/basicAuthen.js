const db = require('../configs/mysql')

const validateClientCredentials = req => {
    // let clientId = req.params.username
    // let clientPassword = req.params.password
    console.log(req.params)
}

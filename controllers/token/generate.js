var gen = require('random-seed') // create a generatorconst

const genToken = (req, res, next) => {
    // console.log('gentoken', req.body)
    // // return 'hello gen xxxxx'

    let token = 'xxxxx'

    return token
}

module.exports = {
    genToken
}
